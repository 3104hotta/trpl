# ch04 所有権 要点メモ

> 見返し用。各節「2〜4行」で要点だけ。詳しい動くコードは `src/bin/NN_*.rs` を参照。

## 4.1 所有権とは？
<!-- 例: 所有権ルール / move / Copy / clone() の要点を学習しながら追記 -->

String型は変更できる。リテラルは変更できない。

参照でわたしたものは変更できない。

所有権読破

## 4.2 参照と借用
<!-- 例: &T は複数OK / &mut T は同時に1つ / 不変と可変は同時不可 / ダングリング防止 -->

ダングリングポインタとは、他人に渡されてしまった可能性のあるメモリを指すポインタのこと。その箇所へのポインタを保持している間に、メモリを解放してしまうことで発生。

参照と借用読破

## 4.3 スライス型
<!-- 例: &str・&[T] は一部への参照で所有権を持たない / first_word をスライスで安全に -->

次回は文字列スライスから
